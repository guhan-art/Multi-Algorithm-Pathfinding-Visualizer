{
  "name": "multi-algorithm-pathfinding-visualizer",
  "version": "1.0.0",
  "private": true,
  "description": "AI course project: Multi-Algorithm Pathfinding Visualizer",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "up": "npm install --prefix backend && npm install --prefix frontend && npm run dev"
  }
}
